jQuery(document).ready(function(){
	jQuery('a.referencia').colorbox({
		inline: true,
		innerWidth: 600,
		innerHeight: 100
	});
	jQuery('a.manual').colorbox({
		inline: true,
		width: 750,
		height: 500
	})
});